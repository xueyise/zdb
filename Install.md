  * Download SSDB source code from http://code.google.com/p/zdb/downloads/list
  * Extract source code, cd the directory
  * Run make to compile
  * Startup ssdb server:
    * `./ssdb-server ssdb.conf`

Logs are written into log.txt in current directory.